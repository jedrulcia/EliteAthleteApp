@using TrainingPlanApp.Web.Constants
@model IEnumerable<TrainingPlanActiveVM>

@{
	ViewData["Title"] = "TrainingPlanActive";
}

<h1>Active Training Plans</h1>

@foreach (var item in Model)
{
	<h4>@item.Name</h4>

	<table class="table">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(model => model.Exercises)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Index)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Sets)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Repeats)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Weight)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Exercises[0].VideoLink)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@for (int i = 0; i < item.Exercises.Count; i++)
			{
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Exercises[i].Name)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Index[i])
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Sets[i])
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Repeats[i])
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Weight[i])
					</td>
					<td>
						<a href="@item.Exercises[i].VideoLink" target="_blank" rel="noopener noreferrer">@item.Exercises[i].VideoLink</a>
					</td>
					<td class="text-end">
						<a asp-action="Details" class="btn btn-secondary btn-sm w-100" asp-route-id="@item.Id"><i class="fa-solid fa-circle-info">&nbsp;</i>Details</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
}

@section Scripts
{
	<script>
		$(function () {
			$('.table').DataTable();
		});
	</script>
}

